NOTE: 一般情况下，我们可以通过对外提供一个http接口完成部署操作，这个http接口接收调用方传递过来的参数(特征数据)，内部调用模型进行预测推理，并将预测结果返回调用方
一般情况下，模型部署的流程如下:
    -1-. 训练好模型后，首先编写好样本的推理预测相关的方法，主要包括两个方法:
        模型恢复的相关方法：推理对象的构造方法，eg:__init__
        模型预测方法：接收入参，入参处理，调用模型，结果处理返回
    -2-. 在推理预测代码的外部套一层"壳"，这层"壳"相关的代码就是一些接收调用方传递特征数据、调用预测代码等相关逻辑
        eg: 本地测试的命令行输入、HTTP接口、RPC接口......-->具体看公司的整体技术框架
    -3-. 相关的测试工作 --> 自己本地的一些测试
    -4-. 选择不同的部署方式，将代码 + 模型文件上传进行部署操作
        eg: 裸服务器的部署、Docker部署、各种第三方的人工智能平台的部署....
        PS: 一般情况下仅需要编写一个Docker项目配置文件(Dockerfile)
        PS: 代码一般情况下使用git进行管理
    -5-. 部署好的服务进行测试check，输出一些文档即可
===============================================================
Linux
    NOTE: 在企业中，一般linux服务器是运维工作人员给你的。一般情况下，会给定IP地址、SSH端口号、用户名、密码；如果学习或者在公司没有提供的时候，可以自行到各种云平台上进行购买使用。
    NOTE: 常用的两个工具: WinSCP(非常方面的将本地文件上传到linux服务器或者从linux服务器下载文件到本地)、putty(可以通过命令行在服务器上进行命令操作)
    参考：
        https://ecs.console.aliyun.com/home#/
        https://winscp.net/eng/docs/lang:chs
        https://www.putty.org/
        https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html
        https://the.earth.li/~sgtatham/putty/latest/w64/putty.zip
        https://www.runoob.com/linux/linux-command-manual.html
        https://www.runoob.com/w3cnote/linux-useful-command.html
    常用命令:
        系统命令：ps、grep、kill、ls、cd、cat、mv、rm、vim、cp、mkdir、wget....
        软件命令：conda、python、pip....
========================================================
HTTP相关内容介绍：
    参考：
        https://www.runoob.com/http/http-methods.html
        https://www.runoob.com/html/html-url.html
    URL路径的解析:
        https://github.com[:port]/pallets/flask[?k1=v1&k2=v2]
            https: 协议schema，eg: http、https
            github.com: 服务器位置，域名、主机名或者ip地址
            /pallets/flask: 资源路径/路由，最终决定具体由那个方法处理当前请求
            port: 可选端口，默认http 80, https 443
            ?k1=v1&k2=v2: get请求时候的参数,此时表示有两个参数k1和k2
        HTTP的请求方式主要有Get、Post
    特殊IP地址:
        0.0.0.0: 表达当前机器的所有IP地址，但是不支持互联网访问0.0.0.0
        127.0.0.1: 当前机器的IP地址
========================================================
Flask
    参考：
        https://pypi.org/project/Flask/
        https://github.com/pallets/flask
        https://flask.palletsprojects.com/en/stable/
        https://flask.palletsprojects.com/zh-cn/stable/
        https://docs.jinkan.org/docs/flask/
    安装命令：
        pip install Flask==3.1.1
    操作：
        1. 首先弄一个Demo，运行看看：在官网的帮助文档中找quickstart
        2. 了解一些基本概念:
            @app.route: 给定一个资源请求路径，也就是路由
            @app.route('/predict', methods=['GET', 'POST'])
            text = request.args.get('text')
            text = request.form.get('text')
            jsonify({'code': 0, 'msg': '成功', 'data': pred_result})
========================================================
基于HTTP API接口 + 裸Linux服务器部署
    参考：
        https://www.runoob.com/linux/linux-command-manual.html
    准备工作：准备一台linux服务器(实际工作的时候，运维会给你的)、下载一些软件WinSCP(可以负责将windows的文件上传到Linux上) + Putty(SSH远程连接的工具)
    步骤：
        1. 通过WinSCP将windows上的代码上传到linux服务器上，放到某个文件夹中即可
        2. 安装Python环境以及依赖库
            wget https://repo.anaconda.com/miniconda/Miniconda3-py310_24.9.2-0-Linux-x86_64.sh
            sh Miniconda3-py310_24.9.2-0-Linux-x86_64.sh
            pip install flask==3.1.1 flask-cors torch==2.3.1 transformers==4.57.3 jieba
        3. 在linux命令行中启动web服务
            # 进入代码所在文件夹
            cd /root/deploy_nlp/text_classify/test
            # 启动服务
            python server_runner.py
            ## PS: 启动服务之后，需要注意服务器的防火墙(服务器是否允许公网IP的访问以及允许那些端口的访问)
            ## 建议启动方式：启动服务器，应该后台运行最好
                nohup python server_runner.py &
        4. 测试
            # 查看进程是否存在
            ps -ef | grep python
(base) root@iZbp1go9moi8408vjeikjiZ:~/deploy_nlp/text_classify/test# ps -ef | grep python
root         895       1  0 22:01 ?        00:00:00 /usr/bin/python3 /usr/bin/networkd-dispatcher --run-startup-triggers
root         965       1  0 22:01 ?        00:00:00 /usr/bin/python3 /usr/share/unattended-upgrades/unattended-upgrade-shutdown --wait-for-signal
root        1078       1  0 22:01 ?        00:00:00 /usr/bin/python3 -Es /usr/sbin/tuned -l -P
root        4595    4384  2 22:15 pts/2    00:00:04 python server_runner.py
root        4693    4652  0 22:18 pts/0    00:00:00 grep --color=auto python

            # 通过curl命令访问测试接口
            curl http://127.0.0.1:5000
        5. 客户端访问
            通过python/java/....语言的http访问库进行访问
========================================================